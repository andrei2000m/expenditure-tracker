pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        google()
    }
}

rootProject.name = "expenditure-tracker"

include(":src:androidMain")
include(":src:androidTest")
